# IRIZA Backend (scaffold)

This folder adds a minimal Node/Express + Mongoose backend scaffold for the IRIZA project.

Quick start:

1. Copy `.env.example` to `.env` and fill values.
2. Install dependencies: `npm install`.
3. Run in development: `npm run dev`.
